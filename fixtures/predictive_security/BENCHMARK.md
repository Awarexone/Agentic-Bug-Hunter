# Benchmark pointer

Canonical benchmark categories and metrics live in
[`docs/predictive/benchmark.md`](../../docs/predictive/benchmark.md).

Fixture cases for that benchmark are under `cases/` in this directory.
